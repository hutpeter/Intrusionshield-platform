{
  "name": "@intrusionshield/platform-workspace",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "clean": "pnpm -r --if-present run clean",
    "lint": "pnpm -r --if-present run lint"
  }
}
